# Local .terraform directories
**/.terraform/*
**/environments/dev/.terraform/*
**/environments/dev/kubeconfig_sm-fargate-ter

# .tfstate files
**/*.tfstate
**/*.tfstate.*

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore terraform.lock files as they are not checked in
.terraform.lock.hcl
